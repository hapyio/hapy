/*Ext.define('MyApp.controller.ViewController', {
    extend: 'Ext.app.Controller',

    config: {
        views: [
            'Main',
        ],

        refs: {
            bView1: 'navigationview button[action="first"]',
            bView2: 'navigationview button[action="second"]',
            nview: '#nvView',
            nbar: '#NavBar',
            bLogin:'#logInButton'
           // bView3: 'itemId = bView3'
        },

        control: {
           /* bView1: {
                tap: 'onView1'
            },
            bView2: {
                tap: 'onView2'
            },
            nview: {

                back: 'onBack'
            }/*,
            bLogin: {
                tap: 'onLogInButtonTap'
            }

        }
    },

    onView1: function(button, e, options) {
        button.up('navigationview').push({
            xtype: 'View2',
            title: 'View2'
        });
        Ext.getCmp('NavBar').hide();


    },
    onBack: function() {
        //alert(Ext.getCmp('nvView').getItems().getCount());
        ///Affichage de la barre uniquement sur la 1ere page de la section
        if(Ext.getCmp('nvView').getItems().getCount() == 4)
        {Ext.getCmp('NavBar').show();}
    },


    onView2: function(button, e, options) {
        button.up('navigationview').push({
            xtype: 'View3',
            title: 'View3'
        });
    }
});
       */